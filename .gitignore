# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Environments / secrets
.env
.venv/
venv/
env/

# OS noise
.DS_Store
Thumbs.db

# Large artifacts (regenerated by `run_all.sh --full`, not version-controlled)
data/
results/models/
results/raw/*
# ...except the small learning-curve trajectories the figures tier needs:
!results/raw/*_encoder_learning_curve.npy

# Generated outputs (regenerated by `run_all.sh --figures`)
results/figures/
results/tables/

# Appendix C throughput CSVs (regenerated by `run_all.sh --full`; the
# throughput tables are transcribed from these by hand)
results/*.csv

# Modal local state
.modal/
